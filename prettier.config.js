const config = {
    trailingComma: 'all',
    tabWidth: 4,
    semi: true,
    singleQuote: true,
    endOfLine: 'lf', //lf-> line feed: used by all UNIX-like environment, if used CRLF instead-> Marks as changes across all file: unnecessary clutter
};

export default config;
